<Window x:Class="LectorGlobalApp.DictationOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DictationOverlayWindow" Height="80" SizeToContent="Width" MinWidth="260" MaxWidth="800"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        WindowStartupLocation="Manual" Loaded="Window_Loaded" SizeChanged="Window_SizeChanged">
    
    <Border Background="#111111" CornerRadius="30" Margin="10" Padding="15,0">
        <Border.Effect>
            <DropShadowEffect Color="Black" Opacity="0.5" BlurRadius="10" ShadowDepth="2"/>
        </Border.Effect>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Boton Cancelar -->
            <Border Grid.Column="0" Background="#333333" Width="36" Height="36" CornerRadius="18" VerticalAlignment="Center" Cursor="Hand" MouseDown="BtnCancel_MouseDown">
                <TextBlock Text="✕" Foreground="White" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-2,0,0"/>
            </Border>

            <!-- Animacion Waveform -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Height="24" Margin="15,0,10,0">
                <StackPanel.Resources>
                    <Style TargetType="Rectangle">
                        <Setter Property="Fill" Value="White"/>
                        <Setter Property="Width" Value="3"/>
                        <Setter Property="RadiusX" Value="1.5"/>
                        <Setter Property="RadiusY" Value="1.5"/>
                        <Setter Property="Margin" Value="2,0"/>
                        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                    </Style>
                </StackPanel.Resources>
                
                <Rectangle x:Name="Bar1" Height="10"/>
                <Rectangle x:Name="Bar2" Height="18"/>
                <Rectangle x:Name="Bar3" Height="24"/>
                <Rectangle x:Name="Bar4" Height="14"/>
                <Rectangle x:Name="Bar5" Height="20"/>
                <Rectangle x:Name="Bar6" Height="12"/>
                <Rectangle x:Name="Bar7" Height="22"/>
                <Rectangle x:Name="Bar8" Height="16"/>
                <Rectangle x:Name="Bar9" Height="8"/>
            </StackPanel>

            <!-- Texto parcial -->
            <TextBlock x:Name="TxtPartial" Grid.Column="2" Foreground="White" FontSize="16" VerticalAlignment="Center" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" MaxWidth="500" Margin="0,0,15,0"/>

            <!-- Boton Confirmar -->
            <Border Grid.Column="3" Background="White" Width="36" Height="36" CornerRadius="18" VerticalAlignment="Center" Cursor="Hand" MouseDown="BtnConfirm_MouseDown">
                <TextBlock Text="✓" Foreground="Black" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-2,0,0"/>
            </Border>
        </Grid>
    </Border>
</Window>
