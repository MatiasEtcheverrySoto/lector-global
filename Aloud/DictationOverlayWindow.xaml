<Window x:Class="LectorGlobalApp.DictationOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DictationOverlayWindow" Height="50" SizeToContent="Width" MinWidth="0" MaxWidth="800"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        WindowStartupLocation="Manual" Loaded="Window_Loaded" SizeChanged="Window_SizeChanged"
        Opacity="0">
    <Border x:Name="MainBorder" Background="#111111" CornerRadius="20" Margin="5" Padding="15,0" Width="100">
        <Border.RenderTransform>
            <TranslateTransform Y="20"/>
        </Border.RenderTransform>
        <Border.Effect>
            <DropShadowEffect Color="Black" Opacity="0.5" BlurRadius="5" ShadowDepth="2"/>
        </Border.Effect>

        <!-- Animacion Waveform -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Height="24">
            <StackPanel.Resources>
                <Style TargetType="Rectangle">
                    <Setter Property="Fill" Value="White"/>
                    <Setter Property="Width" Value="3"/>
                    <Setter Property="RadiusX" Value="1.5"/>
                    <Setter Property="RadiusY" Value="1.5"/>
                    <Setter Property="Margin" Value="2,0"/>
                    <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                </Style>
            </StackPanel.Resources>
            
            <Rectangle x:Name="Bar1" Height="3"/>
            <Rectangle x:Name="Bar2" Height="3"/>
            <Rectangle x:Name="Bar3" Height="3"/>
            <Rectangle x:Name="Bar4" Height="3"/>
            <Rectangle x:Name="Bar5" Height="3"/>
            <Rectangle x:Name="Bar6" Height="3"/>
            <Rectangle x:Name="Bar7" Height="3"/>
            <Rectangle x:Name="Bar8" Height="3"/>
            <Rectangle x:Name="Bar9" Height="3"/>
        </StackPanel>
    </Border>
</Window>
